'use strict';

/**
 * photo-requirements controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::photo-requirements.photo-requirements');

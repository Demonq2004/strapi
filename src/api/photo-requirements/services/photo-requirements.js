'use strict';

/**
 * photo-requirements service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::photo-requirements.photo-requirements');

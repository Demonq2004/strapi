'use strict';

/**
 * photo-requirements router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::photo-requirements.photo-requirements');
